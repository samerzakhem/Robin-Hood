package nfuzion.message.navigation.type;

/**
 * Used to specify the transportation type when creating new Google Routes
 * 
 * @author Lee Becham
 */

enum TransitType
{
   walking;
   driving;    // default
}